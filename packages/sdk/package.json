{
  "name": "@phosop/node",
  "version": "0.2.0",
  "license": "Apache-2.0",
  "description": "Node SDK for Phosop — Stripe-compatible USDC payouts on Solana (devnet + mainnet).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "echo \"no sdk tests\" && exit 0"
  },
  "dependencies": { "@phosop/shared": "workspace:*" },
  "devDependencies": { "@types/node": "^20.12.0", "typescript": "^5.5.0" }
}
